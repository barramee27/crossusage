/** Fork branding — not the upstream OpenUsage app id. */
export const APP_DISPLAY_NAME = "CrossUsage" as const

/** This fork’s GitHub repo (rename your remote repo to match). */
export const FORK_GITHUB_REPO = "barramee27/crossusage" as const
export const FORK_REPO_URL = `https://github.com/${FORK_GITHUB_REPO}` as const

/** Upstream original project (Robin Ebers). */
export const UPSTREAM_REPO_URL = "https://github.com/robinebers/openusage" as const
